import { BrokenLinkDirective } from './broken-link.directive';

describe('BrokenLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BrokenLinkDirective();
    expect(directive).toBeTruthy();
  });
});
